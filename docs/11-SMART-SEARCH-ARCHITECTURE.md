# خطة تطوير نظام البحث الذكي متعدد الطبقات

> تاريخ الإنشاء: 2026-01-08
> آخر تحديث: 2026-01-08
> الحالة: ✅ المرحلة الأولى مكتملة

---

## 🎯 الأهداف الرئيسية

1. **بحث ذكي ودقيق** - نتائج مطابقة 90%+ أو لا نتائج
2. **بحث متعدد الطبقات** - Google Maps → Google Search → Social Media
3. **نافذة مخفية موحدة** - نافذة واحدة تُعاد استخدامها
4. **الإضافة كمحرك تنفيذ فقط** - بدون فورم بحث، عرض التقدم والنتائج فقط
5. **إعدادات مركزية** - تُحفظ في Backend وتُزامن مع الإضافة
6. **بحث Social Media متقدم** - بحث داخلي + Google Search للحسابات

---

## 📊 المشاكل الحالية والحلول

| المشكلة | الحل |
|---------|------|
| إرجاع نتائج غير مطابقة | خوارزمية تطابق صارمة (90%+) مع رفض النتائج الضعيفة |
| فتح نوافذ متعددة | نافذة مخفية واحدة تُعاد استخدامها |
| فورم بحث في الإضافة | إزالته، البحث من المنصة فقط |
| إعدادات مكررة | نظام إعدادات مركزي في Backend |
| بحث Social Media بسيط | بحث متعدد الاستراتيجيات |

---

## 🏗️ البنية المعمارية الجديدة

### 1. طبقات البحث (Search Layers)

```
┌─────────────────────────────────────────────────────────────┐
│                    المنصة (Frontend)                        │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  فورم البحث → إنشاء Job → إرسال للـ Backend         │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Backend (API)                            │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Job Queue → WebSocket → إرسال للإضافة              │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    الإضافة (Extension)                      │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Smart Search Engine (محرك البحث الذكي)             │   │
│  │  ┌─────────────────────────────────────────────┐   │   │
│  │  │ Layer 1: Google Maps                        │   │   │
│  │  │ - بحث دقيق بالاسم                           │   │   │
│  │  │ - تحقق من التطابق (90%+)                    │   │   │
│  │  │ - استخراج: هاتف، موقع، عنوان، تقييم         │   │   │
│  │  └─────────────────────────────────────────────┘   │   │
│  │                      │                             │   │
│  │         (إذا لم يُوجد أو تطابق < 90%)              │   │
│  │                      ▼                             │   │
│  │  ┌─────────────────────────────────────────────┐   │   │
│  │  │ Layer 2: Google Search                      │   │   │
│  │  │ - بحث عن الشركة                             │   │   │
│  │  │ - استخراج: موقع رسمي، حسابات Social Media   │   │   │
│  │  │ - تحقق من التطابق                           │   │   │
│  │  └─────────────────────────────────────────────┘   │   │
│  │                      │                             │   │
│  │         (إذا مُفعّل ومسجل دخول)                    │   │
│  │                      ▼                             │   │
│  │  ┌─────────────────────────────────────────────┐   │   │
│  │  │ Layer 3: Social Media Search                │   │   │
│  │  │ - بحث داخلي في المنصات المتصلة              │   │   │
│  │  │ - بحث Google عن حسابات الشركة               │   │   │
│  │  │ - تحقق متعدد: اسم + username + bio          │   │   │
│  │  └─────────────────────────────────────────────┘   │   │
│  │                      │                             │   │
│  │                      ▼                             │   │
│  │  ┌─────────────────────────────────────────────┐   │   │
│  │  │ Result Merger & Validator                   │   │   │
│  │  │ - دمج النتائج من جميع الطبقات               │   │   │
│  │  │ - حساب درجة الثقة النهائية                  │   │   │
│  │  │ - رفض النتائج < 90%                         │   │   │
│  │  └─────────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 2. نظام النافذة المخفية الموحدة

```javascript
// Window Manager - نافذة واحدة مخفية تُعاد استخدامها
class HiddenWindowManager {
  - windowId: number | null
  - tabId: number | null
  - isReady: boolean
  
  + ensureWindow(): Promise<number>      // إنشاء أو إعادة استخدام
  + navigateTo(url): Promise<void>       // التنقل في نفس الـ tab
  + executeScript(func): Promise<any>    // تنفيذ script
  + closeWindow(): Promise<void>         // إغلاق عند الانتهاء
}
```

### 3. نظام الإعدادات المركزي

```
┌─────────────────────────────────────────────────────────────┐
│                    Backend (Source of Truth)                │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  ExtensionSettings Table                            │   │
│  │  - userId                                           │   │
│  │  - searchLayers: { googleMaps, googleSearch, social}│   │
│  │  - matchThreshold: 90                               │   │
│  │  - maxResults: 30                                   │   │
│  │  - socialPlatforms: { instagram, twitter, ... }     │   │
│  │  - showSearchWindow: false                          │   │
│  │  - searchDelay: 3                                   │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                              │
                    (API: GET/PUT /settings/extension)
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Extension (Cached Copy)                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  chrome.storage.local                               │   │
│  │  - leedz_extension_settings (synced from backend)   │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 📋 خطة التنفيذ المرحلية

### المرحلة 1: إعادة هيكلة الإضافة (Extension Restructure)
**الملفات المتأثرة:**
- `extension/sidepanel.html` - إزالة فورم البحث
- `extension/sidepanel.js` - إبقاء عرض التقدم والنتائج فقط
- `extension/background.js` - إعادة هيكلة Window Manager

**المهام:**
- [ ] إزالة فورم البحث من Side Panel
- [ ] إبقاء: حالة الاتصال، التقدم، النتائج، زر الإعدادات
- [ ] إنشاء HiddenWindowManager class
- [ ] تحديث إدارة النوافذ لاستخدام نافذة واحدة مخفية

---

### المرحلة 2: خوارزمية التطابق الذكية (Smart Matching)
**الملفات المتأثرة:**
- `extension/lib/smart-matcher.js` (جديد)
- `extension/background.js`

**المهام:**
- [ ] إنشاء SmartMatcher class
- [ ] خوارزمية تطابق متعددة المعايير:
  - تطابق الاسم (Levenshtein distance)
  - تطابق الكلمات المفتاحية
  - تطابق المدينة/الموقع
  - تطابق النشاط/الفئة
- [ ] حد أدنى 90% للقبول
- [ ] إرجاع "لا توجد نتائج" إذا لم يتحقق التطابق

---

### المرحلة 3: محرك البحث متعدد الطبقات (Multi-Layer Search Engine)
**الملفات المتأثرة:**
- `extension/search/search-engine.js` (جديد)
- `extension/search/layers/google-maps-layer.js` (جديد)
- `extension/search/layers/google-search-layer.js` (جديد)
- `extension/search/layers/social-media-layer.js` (جديد)

**المهام:**
- [ ] إنشاء SearchEngine class رئيسي
- [ ] Layer 1: Google Maps (تحسين الكود الحالي)
- [ ] Layer 2: Google Search (تحسين الكود الحالي)
- [ ] Layer 3: Social Media (بحث متقدم)
- [ ] Result Merger للدمج والتحقق

---

### المرحلة 4: نظام الإعدادات المركزي (Centralized Settings)
**الملفات المتأثرة:**
- `api/prisma/schema.prisma` - إضافة ExtensionSettings model
- `api/src/settings/` (جديد) - Settings module
- `extension/lib/settings-sync.js` (جديد)
- `web/pages/ExtensionSettingsPage.tsx` (جديد)

**المهام:**
- [ ] إضافة ExtensionSettings model في Prisma
- [ ] إنشاء Settings API endpoints
- [ ] مزامنة الإعدادات في الإضافة
- [ ] صفحة إعدادات في المنصة (popup + صفحة كاملة)

---

### المرحلة 5: البحث المتقدم في Social Media
**الملفات المتأثرة:**
- `extension/search/layers/social-media-layer.js`

**المهام:**
- [ ] بحث داخلي في كل منصة (إذا مسجل دخول)
- [ ] بحث Google عن حسابات الشركة
- [ ] تحقق متعدد: اسم الشركة + username + bio + موقع
- [ ] دمج النتائج مع درجة ثقة

---

### المرحلة 6: تحسين واجهة المنصة
**الملفات المتأثرة:**
- `web/pages/ProspectingPage.tsx`
- `web/components/ExtensionSettingsModal.tsx` (جديد)

**المهام:**
- [ ] تحسين صفحة البحث
- [ ] إضافة زر "إعدادات الإضافة" (popup)
- [ ] عرض حالة الإضافة والاتصال
- [ ] عرض تفصيلي للنتائج مع مصادرها

---

### المرحلة 7: اختبار وتحسين الأداء
**المهام:**
- [ ] اختبار سيناريوهات البحث المختلفة
- [ ] اختبار التطابق الذكي
- [ ] اختبار الأداء والسرعة
- [ ] تحسين معالجة الأخطاء

---

## 🔧 التفاصيل التقنية

### خوارزمية التطابق الذكية

```javascript
class SmartMatcher {
  static THRESHOLD = 90; // الحد الأدنى للقبول
  
  static calculateMatch(searchQuery, result) {
    let score = 0;
    let factors = [];
    
    // 1. تطابق الاسم (40%)
    const nameScore = this.calculateNameMatch(searchQuery.name, result.name);
    score += nameScore * 0.4;
    factors.push({ factor: 'name', score: nameScore });
    
    // 2. تطابق المدينة (25%)
    if (searchQuery.city) {
      const cityScore = this.calculateCityMatch(searchQuery.city, result.address);
      score += cityScore * 0.25;
      factors.push({ factor: 'city', score: cityScore });
    }
    
    // 3. تطابق النشاط (20%)
    if (searchQuery.activity) {
      const activityScore = this.calculateActivityMatch(searchQuery.activity, result.category);
      score += activityScore * 0.2;
      factors.push({ factor: 'activity', score: activityScore });
    }
    
    // 4. وجود معلومات الاتصال (15%)
    const contactScore = this.calculateContactScore(result);
    score += contactScore * 0.15;
    factors.push({ factor: 'contact', score: contactScore });
    
    return {
      totalScore: Math.round(score),
      isMatch: score >= this.THRESHOLD,
      factors,
    };
  }
  
  static calculateNameMatch(query, name) {
    if (!query || !name) return 0;
    
    const q = this.normalize(query);
    const n = this.normalize(name);
    
    // تطابق تام
    if (q === n) return 100;
    
    // يحتوي على الاسم كاملاً
    if (n.includes(q) || q.includes(n)) return 90;
    
    // Levenshtein distance
    const distance = this.levenshtein(q, n);
    const maxLen = Math.max(q.length, n.length);
    const similarity = ((maxLen - distance) / maxLen) * 100;
    
    // تطابق الكلمات
    const wordMatch = this.calculateWordMatch(q, n);
    
    return Math.max(similarity, wordMatch);
  }
  
  static normalize(str) {
    return str
      .toLowerCase()
      .replace(/[^\w\s\u0600-\u06FF]/g, '')
      .replace(/\s+/g, ' ')
      .trim();
  }
  
  static levenshtein(a, b) {
    // Levenshtein distance algorithm
    // ...
  }
  
  static calculateWordMatch(query, name) {
    const queryWords = query.split(' ').filter(w => w.length > 1);
    const nameWords = name.split(' ').filter(w => w.length > 1);
    
    let matchedWords = 0;
    for (const qw of queryWords) {
      for (const nw of nameWords) {
        if (nw.includes(qw) || qw.includes(nw)) {
          matchedWords++;
          break;
        }
      }
    }
    
    return (matchedWords / queryWords.length) * 100;
  }
}
```

### نظام النافذة المخفية

```javascript
class HiddenWindowManager {
  constructor() {
    this.windowId = null;
    this.tabId = null;
    this.isReady = false;
  }
  
  async ensureWindow() {
    // التحقق من وجود النافذة
    if (this.windowId) {
      try {
        await chrome.windows.get(this.windowId);
        return this.windowId;
      } catch {
        this.windowId = null;
        this.tabId = null;
      }
    }
    
    // إنشاء نافذة مخفية جديدة
    const window = await chrome.windows.create({
      type: 'normal',
      focused: false,
      state: 'minimized',
      width: 1400,
      height: 900,
    });
    
    this.windowId = window.id;
    this.tabId = window.tabs[0].id;
    this.isReady = true;
    
    return this.windowId;
  }
  
  async navigateTo(url) {
    await this.ensureWindow();
    await chrome.tabs.update(this.tabId, { url });
    await this.waitForLoad();
  }
  
  async waitForLoad(timeout = 15000) {
    return new Promise((resolve, reject) => {
      const startTime = Date.now();
      
      const checkLoad = async () => {
        try {
          const tab = await chrome.tabs.get(this.tabId);
          if (tab.status === 'complete') {
            resolve();
            return;
          }
          
          if (Date.now() - startTime > timeout) {
            reject(new Error('Page load timeout'));
            return;
          }
          
          setTimeout(checkLoad, 100);
        } catch (error) {
          reject(error);
        }
      };
      
      checkLoad();
    });
  }
  
  async executeScript(func, args = []) {
    await this.ensureWindow();
    const results = await chrome.scripting.executeScript({
      target: { tabId: this.tabId },
      func,
      args,
    });
    return results[0]?.result;
  }
  
  async close() {
    if (this.windowId) {
      try {
        await chrome.windows.remove(this.windowId);
      } catch {}
      this.windowId = null;
      this.tabId = null;
      this.isReady = false;
    }
  }
}
```

---

## 📁 هيكل الملفات الجديد

```
extension/
├── background.js                    # محدث - يستخدم المكونات الجديدة
├── sidepanel.html                   # محدث - بدون فورم بحث
├── sidepanel.js                     # محدث - عرض التقدم والنتائج فقط
├── manifest.json                    # محدث
├── lib/
│   ├── smart-matcher.js             # 🆕 خوارزمية التطابق الذكية
│   ├── hidden-window-manager.js     # 🆕 إدارة النافذة المخفية
│   ├── settings-sync.js             # 🆕 مزامنة الإعدادات
│   ├── selectors.js                 # موجود
│   └── utils.js                     # موجود
├── search/
│   ├── search-engine.js             # 🆕 محرك البحث الرئيسي
│   ├── layers/
│   │   ├── google-maps-layer.js     # 🆕 طبقة Google Maps
│   │   ├── google-search-layer.js   # 🆕 طبقة Google Search
│   │   └── social-media-layer.js    # 🆕 طبقة Social Media
│   ├── google-search.js             # موجود (سيُدمج)
│   ├── orchestrator.js              # موجود (سيُدمج)
│   └── social-media.js              # موجود (سيُدمج)
└── settings/
    ├── settings.html                # موجود
    ├── settings.js                  # محدث - يزامن مع Backend
    └── settings.css                 # موجود

api/
├── prisma/
│   └── schema.prisma                # محدث - ExtensionSettings model
└── src/
    └── extension-settings/          # 🆕 Settings module
        ├── extension-settings.module.ts
        ├── extension-settings.controller.ts
        ├── extension-settings.service.ts
        └── dto/

web/
├── pages/
│   ├── ProspectingPage.tsx          # محدث
│   └── ExtensionSettingsPage.tsx    # 🆕 صفحة إعدادات الإضافة
└── components/
    └── ExtensionSettingsModal.tsx   # 🆕 popup للإعدادات
```

---

## ✅ معايير النجاح

| المعيار | الهدف |
|---------|-------|
| دقة التطابق | 90%+ للنتائج المقبولة |
| رفض النتائج الخاطئة | 100% (لا نتائج غير مطابقة) |
| سرعة البحث الفردي | < 15 ثانية |
| سرعة البحث الجماعي | < 45 ثانية لـ 30 نتيجة |
| استخدام النوافذ | نافذة واحدة مخفية فقط |
| مزامنة الإعدادات | < 2 ثانية |

---

## 🚀 البدء بالتنفيذ

سأبدأ الآن بتنفيذ المرحلة 1: إعادة هيكلة الإضافة
