# 🛠️ Leedz Operations Scripts

> سكربتات التشغيل والإدارة للمشروع

---

## 📋 مصدر الحقيقة الواحد

جميع إعدادات التطوير المحلي موجودة في ملف واحد: **`.env.master`**

```
leedz/
├── .env.master          ← مصدر الحقيقة (لا يُرفع لـ Git)
├── .env.master.example  ← قالب للنسخ
└── ops/
    ├── sync-and-start.ps1   ← مزامنة + تشغيل
    └── update-db-config.ps1 ← تحديث قاعدة البيانات
```

---

## 🚀 البدء السريع

### 1. إعداد الملف (مرة واحدة فقط)

```powershell
Copy-Item .env.master.example .env.master
# ثم عدّل القيم في .env.master
```

### 2. تشغيل المشروع

```powershell
.\ops\sync-and-start.ps1
```

هذا السكربت سيقوم بـ:
- ✅ قراءة الإعدادات من `.env.master`
- ✅ تحديث `api/.env`
- ✅ تحديث `web/.env.local`
- ✅ توليد `extension/config.js`
- ✅ إيقاف أي عمليات على البورتات المطلوبة
- ✅ تشغيل Backend و Frontend

---

## 📝 السكربتات المتاحة

### `sync-and-start.ps1`

السكربت الرئيسي للمزامنة والتشغيل.

```powershell
# مزامنة + تشغيل (الافتراضي)
.\ops\sync-and-start.ps1

# مزامنة فقط بدون تشغيل
.\ops\sync-and-start.ps1 -SyncOnly

# تشغيل فقط بدون مزامنة
.\ops\sync-and-start.ps1 -StartOnly
```

### `update-db-config.ps1`

تحديث جدول `PlatformSettings` في قاعدة البيانات.

```powershell
.\ops\update-db-config.ps1
```

---

## ⚙️ الإعدادات في `.env.master`

```env
# البورتات
API_PORT=3001
WEB_PORT=3000

# قاعدة البيانات
DATABASE_URL=postgresql://...

# المصادقة
JWT_SECRET=...

# إعدادات الإضافة
EXTENSION_DEBUG_MODE=false
EXTENSION_MATCH_THRESHOLD=90
```

---

## 🔄 تغيير البورتات

1. عدّل `.env.master`:
```env
API_PORT=4001
WEB_PORT=4000
```

2. شغّل السكربت:
```powershell
.\ops\sync-and-start.ps1
```

3. (اختياري) حدّث قاعدة البيانات:
```powershell
.\ops\update-db-config.ps1
```

4. أعد تحميل الإضافة في Chrome

---

## 👤 بيانات الدخول للاختبار

| الدور | البريد | كلمة المرور |
|-------|--------|-------------|
| Super Admin | admin@optarget.com | Admin@123 |
| Test User | testuser123@test.com | Test@123 |

---

## 🔗 الروابط الافتراضية

- **Frontend**: http://localhost:3000
- **Backend**: http://localhost:3001
- **API Docs**: http://localhost:3001/api
